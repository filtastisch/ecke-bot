# Discord Bot Token (Pflicht)
TOKEN=DEIN_BOT_TOKEN

# Zeitzone fuer Logs (optional, Standard: Europe/Berlin)
TZ=Europe/Berlin

# Optionale Pfad-Overrides (im Docker-Image bereits sinnvoll gesetzt):
# CONFIG_PATH=/app/data/config.json
# DATA_DIR=/app/data
# HEALTHCHECK_FILE=/tmp/ecke-bot-healthy
